spring:
  datasource:
    url: jdbc:mysql://localhost:3306/client?createDatabaseIfNotExists=true
    username: root
    password: RbzQg1NWrD

  jpa:
    database: mysql
    hibernate:
      ddl-auto: create-drop

  sql:
    init:
      mode: always

logging:
  level:
    org.hibernate.SQL: DEBUG
